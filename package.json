{
  "name": "mtgjson-website",
  "version": "2.0.0",
  "author": "MTGJSON Developers",
  "repository": "https://github.com/mtgjson/mtgjson-website",
  "license": "MIT",
  "engines": {
    "node": "^11.9.0",
    "yarn": "^1.13.0"
  },
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "build:dev": "yarn build && yarn delete-build",
    "delete-build": "rm -rf docs/.vuepress/dist",
    "heroku-postbuild": "yarn build"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "husky": "^1.3.1",
    "pug": "^2.0.3",
    "pug-plain-loader": "^1.0.0",
    "vuepress": "^1.0.0-alpha.37"
  },
  "resolutions": {
    "ajv": "6.8.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn build:dev"
    }
  }
}
